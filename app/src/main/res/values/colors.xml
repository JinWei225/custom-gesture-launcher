<resources>

    <!-- ============================================================================
         Raw values. Only for places that genuinely mean "the literal colour", never
         for theming — everything theme-dependent goes through the md_* tokens below.
         ============================================================================ -->
    <color name="black">#FF000000</color>
    <color name="white">#FFFFFFFF</color>
    <color name="stroke_trail">#FFFFFFFF</color>
    <color name="stroke_preview">#8AFFFFFF</color>

    <!-- Adaptive-icon background. Deliberately has no values-night/ variant: the OS renders
         launcher icons against its own backdrop and never applies our night qualifier to them. -->
    <color name="ic_launcher_background">#101317</color>

    <!-- Dark circle behind the home screen's white drawer glyph. Sits over the wallpaper, so
         like the wallpaper_overlay_* pair below it is intentionally theme-invariant. -->
    <color name="scrim">#66000000</color>

    <!-- ============================================================================
         Wallpaper overlay pair — DELIBERATELY IDENTICAL in values/ and values-night/.

         These style the home screen only: the clock, the date, the hint text, the calendar
         event rows and the drawer glyph. All of those are drawn straight over the user's
         wallpaper, whose brightness has no relationship to the system theme — a light
         wallpaper is just as likely in dark mode as in light. Making them follow the theme
         would tie legibility to a variable that says nothing about what is actually behind
         them, so instead they stay white and lean on the shadow for contrast, which holds
         against any wallpaper.

         If you are here because these look like a missed dark-mode conversion: they aren't.
         ============================================================================ -->
    <color name="wallpaper_overlay_text">#FFFFFFFF</color>
    <color name="wallpaper_overlay_shadow">#B0000000</color>

    <!-- ============================================================================
         Material 3 palette — LIGHT.

         Same key set as values-night/colors.xml, which is what lets themes.xml reference
         these unqualified and stay a single file. Add a token here and you MUST add it
         there too, or `lint` fails with MissingDefaultResource.

         The neutrals are deliberately warm (a paper/cream cast) rather than the pure white
         and blue-grey they used to be: on an OLED phone at low brightness a #FFFFFF sheet is
         genuinely uncomfortable, and warming the whole ramp fixes that without touching
         contrast. Every neutral shares the same hue family, so nothing reads as a stray tint.

         Note the surface/surfaceContainer relationship: in light, `md_surface` (#FBF6EC) is
         *lighter* than `md_surface_container` (#EFE6D5). The window background uses the
         container token and cards/toolbars use surface, so the app reads as warm paper with
         lighter cards floating on it rather than as a flat sheet. In dark that relationship
         inverts, per M3 elevation semantics.

         The accent (md_primary) stays blue: it's the one thing that should read as "this is
         interactive", and it holds against a warm ground better than a warm accent would.
         ============================================================================ -->
    <color name="md_primary">#3B6FD4</color>
    <color name="md_on_primary">#FFFFFF</color>
    <color name="md_primary_container">#D8E2FF</color>
    <color name="md_on_primary_container">#001A43</color>

    <color name="md_secondary_container">#E7DCC3</color>
    <color name="md_on_secondary_container">#2A2318</color>

    <color name="md_surface">#FBF6EC</color>
    <color name="md_on_surface">#221D14</color>
    <color name="md_surface_variant">#E4D9C4</color>
    <color name="md_on_surface_variant">#554C3C</color>

    <color name="md_surface_container_lowest">#FFFDF8</color>
    <color name="md_surface_container_low">#F5EEE0</color>
    <color name="md_surface_container">#EFE6D5</color>
    <color name="md_surface_container_high">#E8DDC8</color>
    <color name="md_surface_container_highest">#E1D5BD</color>

    <color name="md_outline">#7C7361</color>
    <color name="md_outline_variant">#CDC0A8</color>

    <color name="md_error">#BA1A1A</color>
    <color name="md_on_error">#FFFFFF</color>

    <!-- Semantic status colours, kept separate from the accent so they stay readable as
         "pass / fail / not yet run" regardless of what colorPrimary happens to be. -->
    <color name="result_pass">#2E7D32</color>
    <color name="result_fail">#C62828</color>
    <color name="result_neutral">#5F5849</color>

    <!-- Amber is unreadable on a light ground, so light mode gets a darkened variant. -->
    <color name="warning">#B26A00</color>

    <!-- ============================================================================
         Glass surfaces (design: "Glass theme — balanced frost, wallpaper-tinted").

         The drawer and the quick-search card float over the wallpaper instead of sitting on an
         opaque sheet, so their backgrounds are translucent and lean on a blur behind the window.
         Two veils per theme, picked at runtime by ui/Glass.kt: `glass_veil` when the platform
         can actually blur what's behind us (API 31+, cross-window blur enabled), and
         `glass_veil_flat` when it can't — an unblurred wallpaper needs far more cover before
         list text stays readable over it.

         Both are deliberately more opaque than the mock, which assumed a light wallpaper under
         the light theme. These have to hold over any wallpaper in either theme.

         The wallpaper_glass_* pair is theme-invariant for the same reason as
         wallpaper_overlay_* above: it styles the home screen's search button, which is drawn
         straight over the wallpaper. Note that its fill is *dark* while the glass_* veils below
         are light — the veils sit on a window the platform can blur, the button doesn't, so the
         only thing keeping its white glyph legible over a pale wallpaper is the fill itself.
         ============================================================================ -->
    <color name="wallpaper_glass_fill">#73000000</color>
    <color name="wallpaper_glass_stroke">#8CFFFFFF</color>

    <color name="glass_veil">#8CFCF8F0</color>
    <color name="glass_veil_flat">#DBFCF8F0</color>
    <!-- Opaque on purpose: the header's alpha is driven in code, from translucent at rest to
         mostly solid once the list has scrolled under it. -->
    <color name="glass_header">#FFFFFDF8</color>
    <color name="glass_fill">#99FFFFFF</color>
    <color name="glass_stroke">#B3FFFFFF</color>
    <color name="glass_divider">#243C3223</color>

    <!-- Scrim behind the floating quick-search card. Light enough that the app underneath stays
         readable, dark enough to give the card's shadow something to read against. -->
    <color name="quick_search_scrim">#4D000000</color>

</resources>
